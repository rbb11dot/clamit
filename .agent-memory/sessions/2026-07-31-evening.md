# Session Log — 2026-07-31 (evening)

## Bug round 2 (docs-drift + Android bug hunt) — CLOSED

Two scouts ran: `SingleSalamander` (docs vs code drift) and `AndroidBugHunt`
(functional/UI bugs). All findings fixed on `feat/m3-expressive-and-bugfixes`.

### Fixed (backend)
- **Copy-on-write for special days** (the big one): `time_blocks` gained
  nullable `template_id` + `entry_id` (day-owned blocks) with a
  `CHECK ((template_id IS NULL) != (entry_id IS NULL))`. Idempotent Go-side
  migration `ensureDayOwnedBlocks` (db.go) rebuilds old-schema tables (guarded
  by `pragma_table_info`; 001 no longer references `entry_id` so old DBs boot).
  `detachEntryFromTemplateTx` snapshots blocks+subtasks and re-points state
  rows (preserving toggles/status). Wired into `DeleteTemplate`,
  `SetEntryTemplate(null)`, `AddSpecialBlock`; `RemoveSpecialBlock` drops the
  day-owned copy. Verified live: template delete keeps day snapshots, toggles
  survive detach, old-schema migration + FK cascade post-rebuild.
- **`PATCH /api/schedule/{date}/block/{bid}/auto`** registered (spec gap):
  `RecomputeAutoStatus` computes pending/in_progress/completed from clock.

### Fixed (Android)
- BlockEditorPage zero-template save: sequential `ensureTemplateId` →
  `createBlockSuspended` → (home FAB) `addSpecialBlockToCurrentDaySuspended`;
  editor stays open on failure. Stable `SubtaskDraft` uid keys; time
  validation; saving indicator; delete confirmations (block/template);
  `ErrorBanner` on Templates/Blocks pages; `clearError()`.
- `ScheduleViewModel`: `loadGen` race guard (stale responses dropped),
  `mutating` guard (double-tap toggles), `removeSpecialBlockFromDay`.
- `ScheduleScreen`: BackHandler for full-screen editors (dismiss first),
  `blockEditorAddToDay` wiring.
- `ScheduleHomePage`: template chip is a real Surface (no fake FilterChip
  selected state), dead `entry==null` branch → EmptyDayState, remove-from-day
  for special days.
- `TimeBlockCard`: elevation 3dp→2dp (DESIGN.md), rail time wraps at large
  font scales, remove-from-day icon + confirm.
- `ClamitApp`: Koin failures fail fast (no swallow).
- `ApiClient`: base URL from `BuildConfig.API_BASE_URL` (`-PapiBaseUrl`).
- `proguard-rules.pro` (Gson keep), `values-night/themes.xml`, adaptive icon
  grid, `calendar_today` icon, README `/api/health`.

### CI
- Commits: `835b972` (backend cow), `2a6edb3` (android fixes),
  `dfbb957` (PATCH auto + remove-from-day + fail-fast + rail),
  `8c69022` (Close icon import — CI caught missing import).
- All green: run `30630141246` (7/7: backend lint/test/build, android
  lint/test/build). APK at `latest-apk/app-debug.apk` (md5 ea55792b…).
  Format Check `30630141706` green too.

### Not done (explicitly out of scope / deferred)
- Notes, tasks, notifications — deferred by user (schedule first).
- Per-day block EDIT UI and persistent day drag&drop ordering — remaining
  schedule features, not bugs.
- Spec docs (`agent_docs/schedule-spec.md`) still describe the old toggle
  route shape / GET-creates-entry; implementation is source of truth.
- Remaining smoke-test.db file lock on Windows (test artifact; harmless).
