# 2026-07-31

## Session: Maestro E2E setup (Windows + physical device + Termux backend)

### What was done

- Identified video target: Maestro MCP / Maestro CLI ("Claude Code + Maestro MCP + Flutter").
- Installed Maestro CLI 2.7.0 (extracted to `C:\workspace\maestro\bin`), fixed broken
  JAVA_HOME (`C:\Program Files\Eclipse` → Temurin 17 JDK path).
- No emulator: PC shuts down under emulator load; switched to physical Nothing phone
  (serial `00153157O001730`) via Minimal ADB & Fastboot.
- Installed `com.clamit` debug APK (continuous release) via `adb install -r`.
- Started Go backend ON THE PHONE in Termux via adb input injection
  (`input text "clamit"` + `input keyevent 66`); server log confirmed `:8080`.
- App then showed schedule empty state instead of `Failed to connect` error.
- First Maestro flow passed: `smoke.yaml` asserts `"Bu gün için zaman bloğu yok\."`
  (single-quoted regex — double quotes broke YAML parsing).

### Decisions

- Harness drives Maestro via CLI (full path + inline JAVA_HOME); no MCP wiring yet
  (optional for user's own Claude Code: `claude mcp add -s project maestro -- maestro mcp`).

### Pitfalls hit (full list in pitfalls.md)

- JAVA_HOME env vs PowerShell `$env:`; YAML `\.` double-quote escape; screenshot
  timing vs launcher; `am start` without `--user 0`; cp1254 hierarchy output;
  hardcoded `127.0.0.1:8080` (backend must run on phone).

### Next

- Real data flows (schedule: add block → assert → delete) once user confirms direction.
